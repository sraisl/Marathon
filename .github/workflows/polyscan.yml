name: polyscan

on:
  workflow_dispatch:

jobs:
  polyscan:
    runs-on: ubuntu-latest
    steps:

      - name: checkout
        uses: actions/checkout@v7

      - name: polyscan
        uses: sraisl/polyscan-action@v13
        with:
          target: "."
          engines: "semgrep,opengrep,spotbugs,trivy,detekt,gitleaks,hadolint"
          max-critical: "0"
          max-high: "0"
          max-medium: "50"
          gate: "true"
          sarif: "true"
          sbom: "true"
          upload-artifacts: "true"
